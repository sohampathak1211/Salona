import React from 'react'
import useRequest from '../utils/useRequest'

const useAppointment = () => {
    const { Request } = useRequest()
  return (
    <div>useAppointment</div>
  )
}

export default useAppointment